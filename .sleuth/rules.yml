rules:
  - transition-issues-on-deploys:
      conditions:
        - environment='production'
      actions:
        - transition_mentioned_issues:
            state: 'deploy'
        - slack_personal_message:
            group: 'authors'
            message: 'The following Sleuth Actions for <{{deploy_url}}|deploy> failed: {{failed_rules}}'
